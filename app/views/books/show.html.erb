<h1>Détail Book</h1>
<%= render "form", book: @book %>

<%= link_to 'book delete',
  book_path(@book),
  method: :delete,
  data: { confirm: 'Are you sure?' } %>

<%= link_to 'Back to Home', root_path %>
<%= link_to 'book list', books_path, class: 'btn btn-gradient'  %>
